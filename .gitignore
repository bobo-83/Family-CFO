# OS and editors
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# Logs
*.log
logs/

# Environment
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
# `make coverage` writes this next to apps/api/Makefile; it is a binary
# artifact and was committed by accident once.
.coverage
.coverage.*
htmlcov/
.venv/
venv/
dist/
build/
*.egg-info/

# Node / Angular
node_modules/
.angular/
.npm/
coverage/

# Swift / Xcode
DerivedData/
.build/
Packages/
xcuserdata/
*.xcuserstate

# Docker / local runtime
docker/.runtime/
data/
postgres-data/
qdrant-data/
vllm-cache/

# Generated clients and local artifacts
generated/
# … but the committed Swift API client is generated-and-committed by design
# (same discipline as the Angular client; see scripts/generate-swift-client.sh)
!apps/ios/FamilyCFO/FamilyCFO/APIClient/Generated/
tmp/
uv.lock

# Persisted deploy destination (scripts/deploy.sh, scripts/patch.sh)
.deploy.env
.deploy.history

# M121 (ADR 0030): local list of your own literal identifiers to block (never committed).
.repo-hygiene-deny

# Claude Code local state. .claude/worktrees/ holds full checkouts of this repo
# for agents working in isolation — staging one by accident would commit a
# duplicate tree.
.claude/settings.local.json
.claude/worktrees/
